apiVersion: v1
kind: Secret
metadata:
  name: devops-agent-operator-webhook-secret
  namespace: devops-agent-operator-system
  labels:
    app.kubernetes.io/name: devops-agent-operator
    app.kubernetes.io/component: operator
type: Opaque
stringData:
  # AWS DevOps Agent Webhook HMAC secret
  # Replace with your actual webhook secret from AWS DevOps Agent console
  webhook-secret: "DEVOPS_AGENT_WEBHOOK_SECRET"
